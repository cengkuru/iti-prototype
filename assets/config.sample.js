/* Template for assets/config.js (which is gitignored).
   Copy this file to assets/config.js and paste a Mapbox PUBLIC token (pk.).
   Without it, the map renders a self-contained SVG fallback. */
window.ITI_CONFIG = {
  mapboxToken: "" // pk.your-public-token-here
};
